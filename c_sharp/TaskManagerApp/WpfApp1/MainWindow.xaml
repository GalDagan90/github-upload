<Window x:Class="TaskManagerApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskManagerApp"
        xmlns:vm="clr-namespace:TaskManagerApp.ViewModels"
        mc:Ignorable="d"
        Title="Simple Task Manager" Height="650" Width="800">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <Button Content="Add New Task" Padding="5" Margin="0,0,10,0"
                    Command="{Binding AddTaskCommand}"/>
            <Button Content="Delete Selected Task" Padding="5"
                    Command="{Binding DeleteTaskCommand}"/>
        </StackPanel>

        <Border Grid.Row="1" BorderBrush="Black" BorderThickness="2" Margin="10,0,10,10" Padding="10">
            <StackPanel Orientation="Vertical">
                <TextBlock Text="Add New Task" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>

                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Title:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding NewTaskTitle, UpdateSourceTrigger=PropertyChanged}" Width="200"/>

                    <TextBlock Grid.Row="0" Grid.Column="2" Text="Description:" VerticalAlignment="Center" Margin="20,0,10,0"/>
                    <TextBox Grid.Row="0" Grid.Column="3" Text="{Binding NewTaskDescription, UpdateSourceTrigger=PropertyChanged}" Width="200"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Due Date:" VerticalAlignment="Center" Margin="0,10,10,0"/>
                    <DatePicker Grid.Row="1" Grid.Column="1" SelectedDate="{Binding NewTaskDueDate, Mode=TwoWay}" Width="200" HorizontalAlignment="Left" Margin="0,10,0,0"/>

                    <TextBlock Grid.Row="1" Grid.Column="2" Text="Status:" VerticalAlignment="Center" Margin="20,10,10,0"/>
                    <ComboBox Grid.Row="1" Grid.Column="3" ItemsSource="{Binding MyTaskStatuses}"
                              SelectedItem="{Binding NewTaskStatus, Mode=TwoWay}"
                              Width="200" HorizontalAlignment="Left" Margin="0,10,0,0"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Priority:" VerticalAlignment="Center" Margin="0,10,10,0"/>
                    <ComboBox Grid.Row="2" Grid.Column="1" ItemsSource="{Binding MyTaskPriorities}"
                              SelectedItem="{Binding NewTaskPriority, Mode=TwoWay}"
                              Width="200" HorizontalAlignment="Left" Margin="0,10,0,0"/>
                </Grid>
            </StackPanel>
        </Border>

        <ListBox Grid.Row="2" ItemsSource="{Binding Tasks}"
                 SelectedItem="{Binding SelectedTask, Mode=TwoWay}"
                 Margin="10">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Vertical" Margin="0,5,0,5">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Title}" FontWeight="Bold"/>
                            <TextBlock Text=" - "/>
                            <TextBlock Text="{Binding Status}"/>
                            <TextBlock Text=" (Due: "/>
                            <TextBlock Text="{Binding DueDate, StringFormat='{}{0:d}'}"/>
                            <TextBlock Text=")"/>
                            <TextBlock Text=" - Priority: "/>
                            <TextBlock Text="{Binding Priority}"/>
                        </StackPanel>
                        <TextBlock Text="{Binding Description}" TextWrapping="Wrap" FontStyle="Italic" Margin="0,2,0,0"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
